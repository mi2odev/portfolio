# Public origin of the deployed site (no trailing slash). Used for absolute
# Open Graph / Twitter image URLs so link previews work when the URL is shared.
# Netlify (URL) and Vercel (VERCEL_PROJECT_PRODUCTION_URL) are detected automatically.
VITE_SITE_URL=https://your-domain.com
